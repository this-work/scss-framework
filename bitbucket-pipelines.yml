pipelines:
    default:
        - step:
              name: 'Check Commit'
              image: node:16
              script:
                  - npm install
                  - npm run documentation-build
                  - npm run linting
